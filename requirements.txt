# Pinned to versions verified locally so deployment installs the right packages
streamlit==1.51.0
plotly==6.3.1
pandas==2.1.4
numpy==1.26.4
# Streamlit 1.51 pulls PyArrow as a dependency; pin for reproducibility
pyarrow==21.0.0
